# Optional: override the backend URL.
# In dev you can leave this blank — vite.config.js proxies /api → localhost:8000.
# In production, point this at your deployed FastAPI URL:
# VITE_API_BASE=https://api.your-domain.com
VITE_API_BASE=
